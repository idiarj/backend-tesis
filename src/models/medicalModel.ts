import { db } from "../instances/db.js";



export class MedicalModel {
    static async createMedicalRecord({
        victoria,
        puta
    }: {victoria: string, puta: string}): Promise<any> {
        try {

        } catch (error) {

        }
    }

    
}